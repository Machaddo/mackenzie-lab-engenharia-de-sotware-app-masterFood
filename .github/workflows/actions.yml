name: Auto Pull Request Workflow

on:
  push:
    branches:
      - 'feature/**'
      - dev
      - hom
  workflow_dispatch:

jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout do repositório com PAT
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0  # necessário para histórico completo

      # 2️⃣ Determinar branch alvo para o PR
      - name: Determine target branch
        id: target
        run: |
          CURRENT_BRANCH=$(echo $GITHUB_REF | sed 's|refs/heads/||')
          echo "CURRENT_BRANCH=$CURRENT_BRANCH" >> $GITHUB_ENV

          if [[ "$CURRENT_BRANCH" == feature/* ]]; then
            TARGET_BRANCH="dev"
          elif [[ "$CURRENT_BRANCH" == "dev" ]]; then
            TARGET_BRANCH="hom"
          elif [[ "$CURRENT_BRANCH" == "hom" ]]; then
            TARGET_BRANCH="main"
          else
            TARGET_BRANCH=""
          fi

          echo "TARGET_BRANCH=$TARGET_BRANCH" >> $GITHUB_ENV
          echo "Current branch: $CURRENT_BRANCH"
          echo "Target branch: $TARGET_BRANCH"

      # 3️⃣ Criar Pull Request via ação do Peter Evans
      - name: Create Pull Request
        if: env.TARGET_BRANCH != ''
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT_TOKEN }}
          commit-message: "Auto PR from ${{ env.CURRENT_BRANCH }} to ${{ env.TARGET_BRANCH }} [skip ci]"
          branch: ${{ env.CURRENT_BRANCH }}
          base: ${{ env.TARGET_BRANCH }}
          title: "Pull Request: ${{ env.CURRENT_BRANCH }} → ${{ env.TARGET_BRANCH }}"
          body: "Este PR foi gerado automaticamente pelo workflow. Revise e faça o merge manualmente."
          draft: false
